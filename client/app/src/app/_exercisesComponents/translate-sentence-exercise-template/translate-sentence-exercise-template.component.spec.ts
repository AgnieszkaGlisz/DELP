/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TranslateSentenceExerciseTemplateComponent } from './translate-sentence-exercise-template.component';

describe('TranslateSentenceExerciseTemplateComponent', () => {
  let component: TranslateSentenceExerciseTemplateComponent;
  let fixture: ComponentFixture<TranslateSentenceExerciseTemplateComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TranslateSentenceExerciseTemplateComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TranslateSentenceExerciseTemplateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
