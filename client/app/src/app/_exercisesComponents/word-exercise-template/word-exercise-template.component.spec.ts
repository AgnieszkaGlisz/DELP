/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { WordExerciseTemplateComponent } from './word-exercise-template.component';

describe('WordExerciseTemplateComponent', () => {
  let component: WordExerciseTemplateComponent;
  let fixture: ComponentFixture<WordExerciseTemplateComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ WordExerciseTemplateComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(WordExerciseTemplateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
